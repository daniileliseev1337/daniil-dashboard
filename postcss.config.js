// PostCSS — это посредник между нашими CSS-файлами и финальным результатом.
// Через него подключаются два инструмента: Tailwind (превращает классы вроде
// "px-4 py-2" в реальный CSS) и Autoprefixer (добавляет префиксы для
// совместимости со старыми браузерами, например -webkit- для Safari).
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
