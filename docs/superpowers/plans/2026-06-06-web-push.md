# Web Push Notifications Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Доставлять системные push-уведомления (Web Push / VAPID) о событиях приложения на Desktop/Android/iOS, заменяя заблокированный Telegram-канал.

**Architecture:** PWA (vite-plugin-pwa `injectManifest`) + кастомный service worker для приёма push; подписки в таблице `push_subscriptions` (RLS); edge-функция `web-push-notify` (@negrel/webpush) резолвит получателей по `profiles.notif_*` и шлёт зашифрованный payload; deadline через pg_cron+pg_net на `kong:8000`.

**Tech Stack:** React+Vite 5, vite-plugin-pwa, @negrel/webpush (Deno/JSR), self-hosted Supabase (PG17, edge-runtime, kong), pg_cron/pg_net.

**Спек:** `docs/superpowers/specs/2026-06-06-web-push-notifications-design.md`.

**Грабли среды (из памяти проекта):** git ТОЛЬКО с Windows-стороны (`git -C "F:\Сайт\redesign-v2-fresh" -c safe.directory=*`); push с обходом прокси (`$env:HTTPS_PROXY=""` + `git -c http.proxy=""`); НЕ делать `wsl --shutdown` без перезапуска VBS-держателя; сложный bash в WSL — через файл-скрипт (`\\wsl.localhost\Ubuntu\...`), не inline. Edge/БД-команды — `docker exec` в WSL.

---

## Файловая структура

**Создаются:**
- `supabase/migrations/20260606_0001_push_subscriptions.sql` — таблица + RLS + индексы.
- `supabase/migrations/20260606_0002_notif_new_project.sql` — колонка флага + правка `update_notification_settings`.
- `supabase/migrations/20260606_0003_pg_cron_deadline.sql` — extensions + cron job.
- `deploy/web-push/functions/web-push-notify/index.ts` — edge-функция.
- `deploy/web-push/functions/web-push-notify/config.example.json` — шаблон секрета (без значений).
- `deploy/web-push/deploy-edge-function.sh` — деплой edge (по образцу nextcloud).
- `deploy/web-push/verify-rls.sh` — проверка RLS push_subscriptions.
- `deploy/web-push/gen-vapid.ts` — генерация VAPID-ключей.
- `src/sw.js` — service worker (push + notificationclick + precache).
- `src/lib/push.js` — фронт-модуль подписки.
- `public/manifest.webmanifest` — PWA-манифест.
- `public/icon-192.png`, `public/icon-512.png`, `public/apple-touch-icon.png` — иконки.
- `scripts/gen-icons.mjs` — генерация иконок.

**Модифицируются:**
- `vite.config.js` — подключить vite-plugin-pwa (injectManifest).
- `index.html` — `<link rel=manifest>`, apple-meta.
- `src/App.jsx` — регистрация SW, обёртка `sendPush`, точки вызова, UI настроек, iOS-баннер.
- `.env.production` — `VITE_VAPID_PUBLIC_KEY`.
- `package.json` — devDeps (`vite-plugin-pwa`, `workbox-*`, `sharp`).

---

## Фаза 1 — БД: push_subscriptions + флаг + cron-extensions

### Task 1: Миграция таблицы push_subscriptions

**Files:**
- Create: `supabase/migrations/20260606_0001_push_subscriptions.sql`

- [ ] **Step 1: Написать миграцию**

```sql
-- Web Push: подписки браузеров пользователей.
create table if not exists public.push_subscriptions (
  id          uuid primary key default gen_random_uuid(),
  user_id     uuid not null references auth.users(id) on delete cascade,
  endpoint    text not null unique,
  p256dh      text not null,
  auth        text not null,
  user_agent  text,
  created_at  timestamptz not null default now()
);
create index if not exists push_subscriptions_user_id_idx on public.push_subscriptions(user_id);

alter table public.push_subscriptions enable row level security;

-- свои подписки: чтение/вставка/удаление
create policy push_sub_select_own on public.push_subscriptions
  for select using (user_id = auth.uid());
create policy push_sub_insert_own on public.push_subscriptions
  for insert with check (user_id = auth.uid());
create policy push_sub_delete_own on public.push_subscriptions
  for delete using (user_id = auth.uid());
-- update не нужен (подписка пересоздаётся upsert'ом on conflict endpoint)
```

- [ ] **Step 2: Применить к живой БД**

Записать скрипт `\\wsl.localhost\Ubuntu\root\apply-push-mig.sh`:
```bash
#!/usr/bin/env bash
set -euo pipefail
docker exec -i supabase-db psql -U postgres -d postgres < /mnt/f/Сайт/redesign-v2-fresh/supabase/migrations/20260606_0001_push_subscriptions.sql
```
Run (PowerShell): `wsl -d Ubuntu -u root -- bash /root/apply-push-mig.sh`
Expected: `CREATE TABLE`, `CREATE INDEX`, `ALTER TABLE`, 3×`CREATE POLICY`.

- [ ] **Step 3: Проверить структуру**

Run: `wsl -d Ubuntu -u root -- docker exec supabase-db psql -U postgres -d postgres -c "\d public.push_subscriptions"`
Expected: колонки id/user_id/endpoint/p256dh/auth/user_agent/created_at; unique на endpoint; rowsecurity = true.

- [ ] **Step 4: Commit**

```
git -C "F:\Сайт\redesign-v2-fresh" -c safe.directory=* add supabase/migrations/20260606_0001_push_subscriptions.sql
git -C "F:\Сайт\redesign-v2-fresh" -c safe.directory=* commit -m "feat(push): таблица push_subscriptions + RLS"
```

### Task 2: verify-rls.sh для push_subscriptions

**Files:**
- Create: `deploy/web-push/verify-rls.sh`

- [ ] **Step 1: Написать проверку** (по образцу `deploy/tasks/verify-rls.sh`)

Скрипт под двумя JWT (юзер A, юзер B): A вставляет подписку (endpoint='https://t/A'), B НЕ видит её через PostgREST select (RLS), A видит свою. Печатает `RLS_OK` или `RLS_FAIL`.

```bash
#!/usr/bin/env bash
# Проверка RLS push_subscriptions под двумя пользователями.
# Использует те же helper'ы получения JWT, что deploy/tasks/verify-rls.sh
# (скопировать механику генерации токенов A/B оттуда).
set -euo pipefail
# ... (JWT_A, JWT_B получаются как в deploy/tasks/verify-rls.sh)
BASE="http://kong:8000/rest/v1/push_subscriptions"
# A вставляет
curl -s -X POST "$BASE" -H "Authorization: Bearer $JWT_A" -H "apikey: $ANON" \
  -H "Content-Type: application/json" \
  -d '{"user_id":"'"$UID_A"'","endpoint":"https://t/A","p256dh":"x","auth":"y"}' >/dev/null
# B пытается прочитать A
SEEN=$(curl -s "$BASE?endpoint=eq.https://t/A" -H "Authorization: Bearer $JWT_B" -H "apikey: $ANON")
# A читает свою
OWN=$(curl -s "$BASE?endpoint=eq.https://t/A" -H "Authorization: Bearer $JWT_A" -H "apikey: $ANON")
if [ "$SEEN" = "[]" ] && echo "$OWN" | grep -q "https://t/A"; then echo "RLS_OK"; else echo "RLS_FAIL B=$SEEN A=$OWN"; fi
# cleanup
curl -s -X DELETE "$BASE?endpoint=eq.https://t/A" -H "Authorization: Bearer $JWT_A" -H "apikey: $ANON" >/dev/null
```

- [ ] **Step 2: Запустить**

Run: `wsl -d Ubuntu -u root -- bash /mnt/f/Сайт/redesign-v2-fresh/deploy/web-push/verify-rls.sh`
Expected: `RLS_OK`.

- [ ] **Step 3: Commit**
```
git ... add deploy/web-push/verify-rls.sh
git ... commit -m "test(push): verify-rls для push_subscriptions"
```

### Task 3: Миграция notif_new_project

**Files:**
- Create: `supabase/migrations/20260606_0002_notif_new_project.sql`

- [ ] **Step 1: Написать миграцию**

```sql
-- Флаг broadcast-уведомления о новых проектах в поиске исполнителя.
alter table public.profiles
  add column if not exists notif_new_project boolean not null default true;
```

Примечание: RPC `update_notification_settings` (5-арг, см. память 6.4a) расширить
параметром `p_notif_new_project boolean default true` — но т.к. функция живёт
ТОЛЬКО в живой БД (техдолг: нет в репо-миграциях), её правку зафиксировать
отдельным файлом миграции с полным `CREATE OR REPLACE FUNCTION` (вытащить текущее
тело через `\sf update_notification_settings` перед правкой). Если решено не
трогать RPC — фронт пишет `notif_new_project` прямым update под RLS (как уже
делает `update` profiles в `App.jsx:5572`).

- [ ] **Step 2: Применить и проверить**

Run: применить через docker exec (как Task 1 Step 2).
Run: `wsl -d Ubuntu -u root -- docker exec supabase-db psql -U postgres -d postgres -c "select column_name from information_schema.columns where table_name='profiles' and column_name='notif_new_project'"`
Expected: одна строка `notif_new_project`.

- [ ] **Step 3: Commit**
```
git ... add supabase/migrations/20260606_0002_notif_new_project.sql
git ... commit -m "feat(push): флаг notif_new_project в profiles"
```

### Task 4: pg_cron + pg_net extensions (проверка/включение)

**Files:**
- Create: `supabase/migrations/20260606_0003_pg_cron_deadline.sql` (cron job добавится в Фазе 6 — здесь только extensions)

- [ ] **Step 1: Проверить наличие extensions**

Run: `wsl -d Ubuntu -u root -- docker exec supabase-db psql -U postgres -d postgres -c "select extname from pg_extension where extname in ('pg_cron','pg_net')"`
Expected: либо обе строки (уже есть), либо пусто (нужно включить).

- [ ] **Step 2: Если отсутствуют — включить**

```sql
create extension if not exists pg_cron;
create extension if not exists pg_net;
```
ВАЖНО: pg_cron требует загрузки в `shared_preload_libraries` (postgresql.conf) и
работает в БД, указанной `cron.database_name` (по умолчанию `postgres`). Если
`create extension pg_cron` падает с "pg_cron can only be loaded via shared_preload_libraries"
— добавить `pg_cron` в `shared_preload_libraries` в конфиге контейнера PG17
(`docker-compose.pg17.yml` / postgresql.conf), пересоздать контейнер db, повторить.
Зафиксировать в `deploy/INFRASTRUCTURE.md`.

- [ ] **Step 3: Commit** (файл миграции с `create extension` строками)
```
git ... add supabase/migrations/20260606_0003_pg_cron_deadline.sql
git ... commit -m "feat(push): включить pg_cron + pg_net для deadline-cron"
```

---

## Фаза 2 — PWA + Service Worker

### Task 5: Установить зависимости

**Files:**
- Modify: `package.json`

- [ ] **Step 1: Установить devDeps** (Windows-сторона)

Run (PowerShell): `Set-Location "F:\Сайт\redesign-v2-fresh"; npm install -D vite-plugin-pwa workbox-precaching workbox-routing sharp`
Expected: добавлены в devDependencies, `npm ci` воспроизводим.

- [ ] **Step 2: Commit**
```
git ... add package.json package-lock.json
git ... commit -m "build(push): vite-plugin-pwa + workbox + sharp"
```

### Task 6: Service worker (push + notificationclick)

**Files:**
- Create: `src/sw.js`

- [ ] **Step 1: Написать SW**

```js
// Service Worker: precache app-shell (workbox) + приём Web Push.
import { precacheAndRoute } from 'workbox-precaching'

precacheAndRoute(self.__WB_MANIFEST || [])

self.addEventListener('push', (event) => {
  if (!event.data) return
  let payload = {}
  try { payload = event.data.json() } catch { payload = { title: 'Уведомление', body: event.data.text() } }
  const { title = 'КЛИМАТ-ПРО', body = '', url = '/', tag } = payload
  event.waitUntil(
    self.registration.showNotification(title, {
      body,
      tag,
      icon: '/icon-192.png',
      badge: '/icon-192.png',
      data: { url },
    })
  )
})

self.addEventListener('notificationclick', (event) => {
  event.notification.close()
  const url = (event.notification.data && event.notification.data.url) || '/'
  event.waitUntil((async () => {
    const all = await self.clients.matchAll({ type: 'window', includeUncontrolled: true })
    const focused = all.find((c) => 'focus' in c)
    if (focused) { await focused.focus(); if ('navigate' in focused) await focused.navigate(url) }
    else await self.clients.openWindow(url)
  })())
})
```

- [ ] **Step 2: Commit** (SW заработает после Task 7 — vite config)
```
git ... add src/sw.js
git ... commit -m "feat(push): service worker — push + notificationclick"
```

### Task 7: vite-plugin-pwa (injectManifest)

**Files:**
- Modify: `vite.config.js`

- [ ] **Step 1: Подключить плагин**

В `vite.config.js` импорт и плагин:
```js
import { VitePWA } from 'vite-plugin-pwa'
// ... в plugins: [...]
VitePWA({
  strategies: 'injectManifest',
  srcDir: 'src',
  filename: 'sw.js',
  registerType: 'autoUpdate',
  injectRegister: null, // регистрируем вручную через useRegisterSW в App
  manifest: false,      // используем статический public/manifest.webmanifest
  injectManifest: { swSrc: 'src/sw.js', swDest: 'dist/sw.js' },
  devOptions: { enabled: false },
})
```

- [ ] **Step 2: Сборка проверяет, что SW собирается**

Run: `Set-Location "F:\Сайт\redesign-v2-fresh"; npm run build`
Expected: `✓ built`; в `dist/` присутствует `sw.js` с инжектированным precache-манифестом (`self.__WB_MANIFEST` заменён массивом).

- [ ] **Step 3: Commit**
```
git ... add vite.config.js
git ... commit -m "build(push): vite-plugin-pwa injectManifest"
```

### Task 8: PWA manifest + иконки + index.html

**Files:**
- Create: `public/manifest.webmanifest`, `scripts/gen-icons.mjs`, `public/icon-192.png`, `public/icon-512.png`, `public/apple-touch-icon.png`
- Modify: `index.html`

- [ ] **Step 1: Манифест**

```json
{
  "name": "КЛИМАТ-ПРО",
  "short_name": "КЛИМАТ-ПРО",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#0a0a0a",
  "theme_color": "#0a0a0a",
  "icons": [
    { "src": "/icon-192.png", "sizes": "192x192", "type": "image/png" },
    { "src": "/icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
  ]
}
```

- [ ] **Step 2: Генератор иконок** `scripts/gen-icons.mjs` (sharp; простой логотип — буква «К» на тёмном фоне)

```js
import sharp from 'sharp'
const svg = (s) => Buffer.from(
  `<svg xmlns="http://www.w3.org/2000/svg" width="${s}" height="${s}">
   <rect width="100%" height="100%" rx="${s*0.18}" fill="#0a0a0a"/>
   <text x="50%" y="54%" font-family="Geist,Arial" font-size="${s*0.6}" fill="#93c5fd"
   text-anchor="middle" dominant-baseline="middle" font-weight="700">К</text></svg>`)
for (const [name, size] of [['icon-192',192],['icon-512',512],['apple-touch-icon',180]])
  await sharp(svg(size)).png().toFile(`public/${name}.png`)
console.log('icons generated')
```
Run: `node scripts/gen-icons.mjs`
Expected: 3 PNG в `public/`.

- [ ] **Step 3: index.html meta**

Добавить в `<head>`:
```html
<link rel="manifest" href="/manifest.webmanifest" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
<meta name="apple-mobile-web-app-capable" content="yes" />
<meta name="apple-mobile-web-app-status-bar-style" content="black" />
```

- [ ] **Step 4: Сборка + commit**
Run: `npm run build` → Expected: manifest и иконки в `dist/`.
```
git ... add public/manifest.webmanifest public/icon-192.png public/icon-512.png public/apple-touch-icon.png scripts/gen-icons.mjs index.html
git ... commit -m "feat(push): PWA manifest + иконки + apple-meta"
```

---

## Фаза 3 — Фронт: подписка + настройки

### Task 9: VAPID-ключи + env

**Files:**
- Create: `deploy/web-push/gen-vapid.ts`
- Modify: `.env.production` (public-ключ; private — на сервер в config.json вручную)

- [ ] **Step 1: Скрипт генерации**

```ts
// deno run deploy/web-push/gen-vapid.ts  — печатает publicKey/privateKey (JWK).
import * as webpush from "jsr:@negrel/webpush";
const keys = await webpush.generateVapidKeys({ extractable: true });
const exported = await webpush.exportVapidKeys(keys);
console.log(JSON.stringify(exported, null, 2));
```
Run (в WSL, где есть deno; иначе через edge-runtime контейнер):
`wsl -d Ubuntu -u root -- bash -lc "cd /mnt/f/Сайт/redesign-v2-fresh && deno run deploy/web-push/gen-vapid.ts"`
Expected: JSON с publicKey и privateKey. **Сохранить вне git.**

- [ ] **Step 2: Положить ключи**
- Публичный (base64url, для `applicationServerKey`) → `.env.production`: `VITE_VAPID_PUBLIC_KEY=<public>`.
- Приватный (JWK) → позже в `config.json` edge (Task 13). НЕ коммитить.

- [ ] **Step 3: Commit** (только скрипт; ключи не коммитятся)
```
git ... add deploy/web-push/gen-vapid.ts
git ... commit -m "feat(push): генератор VAPID-ключей (@negrel/webpush)"
```

### Task 10: Фронт-модуль push.js

**Files:**
- Create: `src/lib/push.js`

- [ ] **Step 1: Написать модуль**

```js
// Web Push: подписка/отписка браузера + сохранение в push_subscriptions.
const VAPID_PUBLIC = import.meta.env.VITE_VAPID_PUBLIC_KEY

function urlBase64ToUint8Array(base64) {
  const padding = '='.repeat((4 - (base64.length % 4)) % 4)
  const b64 = (base64 + padding).replace(/-/g, '+').replace(/_/g, '/')
  const raw = atob(b64)
  return Uint8Array.from([...raw].map((c) => c.charCodeAt(0)))
}

export function isPushSupported() {
  return 'serviceWorker' in navigator && 'PushManager' in window && 'Notification' in window
}

export async function getPushState() {
  if (!isPushSupported()) return { supported: false, subscribed: false }
  const reg = await navigator.serviceWorker.ready
  const sub = await reg.pushManager.getSubscription()
  return { supported: true, subscribed: !!sub, permission: Notification.permission }
}

export async function enablePush(client) {
  if (!isPushSupported()) throw new Error('Push не поддерживается этим браузером')
  const perm = await Notification.requestPermission()
  if (perm !== 'granted') throw new Error('Нет разрешения на уведомления')
  const reg = await navigator.serviceWorker.ready
  const sub = await reg.pushManager.subscribe({
    userVisibleOnly: true,
    applicationServerKey: urlBase64ToUint8Array(VAPID_PUBLIC),
  })
  const json = sub.toJSON()
  const { data: { user } } = await client.auth.getUser()
  const { error } = await client.from('push_subscriptions').upsert({
    user_id: user.id,
    endpoint: sub.endpoint,
    p256dh: json.keys.p256dh,
    auth: json.keys.auth,
    user_agent: navigator.userAgent.slice(0, 200),
  }, { onConflict: 'endpoint' })
  if (error) throw error
  return true
}

export async function disablePush(client) {
  const reg = await navigator.serviceWorker.ready
  const sub = await reg.pushManager.getSubscription()
  if (!sub) return
  await client.from('push_subscriptions').delete().eq('endpoint', sub.endpoint)
  await sub.unsubscribe()
}
```

- [ ] **Step 2: Commit**
```
git ... add src/lib/push.js
git ... commit -m "feat(push): фронт-модуль подписки push.js"
```

### Task 11: Регистрация SW + UI настроек + iOS-баннер

**Files:**
- Modify: `src/App.jsx`

- [ ] **Step 1: Регистрация SW** — в корневом компоненте App один `useEffect`:

```js
// при старте: зарегистрировать SW (push-канал). vite-plugin-pwa отдаёт /sw.js.
useEffect(() => {
  if ('serviceWorker' in navigator) {
    navigator.serviceWorker.register('/sw.js').catch((e) => console.warn('SW reg failed', e))
  }
}, [])
```

- [ ] **Step 2: Блок «Уведомления» в SettingsView** (рядом с существующими `notif_*`, ~5512-5576):
  - Импорт: `import { isPushSupported, getPushState, enablePush, disablePush } from './lib/push'`.
  - Тумблер «Push на этом устройстве»: при вкл → `enablePush(client)`, при выкл → `disablePush(client)`; состояние из `getPushState()`; при `permission==='denied'` — текст «Уведомления заблокированы в браузере, разрешите в настройках сайта».
  - Галочка `notif_new_project` (новый флаг) — добавить в существующий объект настроек (по образцу `notifTask` в `App.jsx:5516,5576`): чтение `profile?.notif_new_project !== false`, запись `notif_new_project: updated.notifNewProject`.

- [ ] **Step 3: iOS install-баннер** — компонент, рендерится когда:
  `/iphone|ipad|ipod/i.test(navigator.userAgent) && !navigator.standalone`:
  текст «Чтобы получать уведомления на iPhone: Поделиться → На экран „Домой"».

- [ ] **Step 4: Сборка + commit**
Run: `npm run build` → Expected: зелёная сборка.
```
git ... add src/App.jsx
git ... commit -m "feat(push): регистрация SW, настройки push, iOS-баннер"
```

---

## Фаза 4 — Edge-функция web-push-notify

### Task 12: Edge-функция (транспорт + резолверы)

**Files:**
- Create: `deploy/web-push/functions/web-push-notify/index.ts`, `deploy/web-push/functions/web-push-notify/config.example.json`

- [ ] **Step 1: config.example.json** (шаблон, без значений)
```json
{ "subject": "mailto:CHANGE_ME@example.com", "vapidKeys": { "publicKey": "", "privateKey": "" } }
```

- [ ] **Step 2: Написать edge-функцию** (по образцу `deploy/tasks/functions/telegram-notify/index.ts`)

```ts
// Edge Function web-push-notify — Web Push уведомления (замена telegram-notify).
// Секрет (VAPID) — в config.json рядом (НЕ в git). PostgREST под service_role
// через SUPABASE_URL=http://kong:8000. Фильтр получателей по profiles.notif_*.
import cfg from "./config.json" with { type: "json" };
import * as webpush from "jsr:@negrel/webpush";

const SUPABASE_URL = Deno.env.get("SUPABASE_URL")!;
const SERVICE_KEY  = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;

const CORS = {
  "Access-Control-Allow-Origin": "*",
  "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
  "Access-Control-Allow-Methods": "POST, OPTIONS",
};
const j = (b: unknown, s = 200) =>
  new Response(JSON.stringify(b), { status: s, headers: { ...CORS, "Content-Type": "application/json" } });

function rest(path: string, init?: RequestInit) {
  return fetch(`${SUPABASE_URL}/rest/v1/${path}`, {
    ...init,
    headers: { apikey: SERVICE_KEY, Authorization: `Bearer ${SERVICE_KEY}`, ...(init?.headers || {}) },
  });
}

// VAPID application server (один раз на холодный старт)
const appServer = await webpush.ApplicationServer.new({
  contactInformation: cfg.subject,
  vapidKeys: await webpush.importVapidKeys(cfg.vapidKeys, { extractable: false }),
});

// получатели по id-списку + флагу notif_*, минус инициатор
async function recipients(ids: (string|null|undefined)[], initiator: string|undefined, flag: string) {
  const uniq = [...new Set(ids.filter(Boolean).filter((x) => x !== initiator))] as string[];
  if (!uniq.length) return [] as string[];
  const inList = uniq.map((x) => `"${x}"`).join(",");
  const r = await rest(`profiles?id=in.(${inList})&${flag}=eq.true&select=id`);
  const rows = await r.json();
  return Array.isArray(rows) ? rows.map((p) => p.id) : [];
}

// все одобренные минус владелец (broadcast)
async function broadcastApproved(ownerId: string|undefined, flag: string) {
  const r = await rest(`profiles?approved=eq.true&${flag}=eq.true&select=id`);
  const rows = await r.json();
  return Array.isArray(rows) ? rows.map((p) => p.id).filter((id) => id !== ownerId) : [];
}

async function sendToUsers(userIds: string[], payload: object) {
  if (!userIds.length) return 0;
  const inList = userIds.map((x) => `"${x}"`).join(",");
  const r = await rest(`push_subscriptions?user_id=in.(${inList})&select=endpoint,p256dh,auth`);
  const subs = await r.json();
  if (!Array.isArray(subs)) return 0;
  let sent = 0;
  for (const s of subs) {
    const subscriber = appServer.subscribe({ endpoint: s.endpoint, keys: { p256dh: s.p256dh, auth: s.auth } });
    try {
      await subscriber.pushTextMessage(JSON.stringify(payload), {});
      sent++;
    } catch (e) {
      const code = (e as { statusCode?: number }).statusCode;
      if (code === 404 || code === 410) {
        await rest(`push_subscriptions?endpoint=eq.${encodeURIComponent(s.endpoint)}`, { method: "DELETE" });
      } else { console.warn("push send error", String(e)); }
    }
  }
  return sent;
}
```

Далее `Deno.serve` с роутингом по `type` (резолверы из §5 спека):
```ts
Deno.serve(async (req) => {
  if (req.method === "OPTIONS") return new Response("ok", { headers: CORS });
  const b = await req.json().catch(() => ({} as Record<string, unknown>));
  const type = b.type as string;
  const initiator = b.initiatorId as string | undefined;
  try {
    if (type === "task_assigned" || type === "task_status" || type === "task_created" || type === "deadline") {
      // task_* : резолв по project_tasks (title/project_id/author_id/assigned_to/status) — как telegram-notify;
      // deadline: select задач с due в ближайшие 24ч (поле срока — уточнить имя в схеме project_tasks),
      //           для каждой шлём author_id+assigned_to с флагом notif_deadline.
      // payload {title, body, url:`/tasks/${taskId}`, tag:`task-${taskId}`}
      // ... (полная логика — перенести структуру из telegram-notify, заменив sendTo на sendToUsers)
      return j({ ok: true });
    }
    if (type === "project_taken" || type === "team_invite") {
      const flag = type === "project_taken" ? "notif_project_taken" : "notif_team_invite";
      const ids = await recipients([b.recipientId as string], initiator, flag);
      const text = type === "project_taken"
        ? `✅ Ваш проект взят в работу`
        : `👥 Вас пригласили в команду проекта`;
      const sent = await sendToUsers(ids, { title: "КЛИМАТ-ПРО", body: text, url: "/" });
      return j({ ok: true, sent });
    }
    if (type === "comment") {
      // получатели: author задачи + assigned_to + участники проекта (через can_access_task / project_members), минус initiator
      const taskId = b.taskId as string;
      const tr = await rest(`project_tasks?id=eq.${taskId}&select=title,project_id,author_id,assigned_to`);
      const task = (await tr.json())?.[0];
      if (!task) return j({ ok: true, note: "task not found" });
      const ids = await recipients([task.author_id, task.assigned_to], initiator, "notif_comment");
      const sent = await sendToUsers(ids, { title: "КЛИМАТ-ПРО", body: `💬 Новый комментарий: ${task.title}`, url: `/tasks/${taskId}`, tag: `task-${taskId}` });
      return j({ ok: true, sent });
    }
    if (type === "project_published") {
      const ids = await broadcastApproved(b.ownerId as string, "notif_new_project");
      const sent = await sendToUsers(ids, { title: "КЛИМАТ-ПРО", body: `🆕 Новый проект в поиске исполнителя`, url: "/" });
      return j({ ok: true, sent });
    }
    return j({ ok: true, note: "unknown type" });
  } catch (e) {
    return j({ error: String(e) }, 500);
  }
});
```

ПРИМЕЧАНИЕ исполнителю: точные API `@negrel/webpush` (`ApplicationServer.new`,
`importVapidKeys`, `subscribe`, `pushTextMessage`) сверить с README библиотеки и
примером `callmedeci/supabase-webpush` ПЕРЕД написанием — сигнатуры могли
измениться. Если расходятся — адаптировать вызовы, логика резолверов не меняется.

- [ ] **Step 3: Commit**
```
git ... add deploy/web-push/functions/web-push-notify/index.ts deploy/web-push/functions/web-push-notify/config.example.json
git ... commit -m "feat(push): edge-функция web-push-notify (@negrel/webpush)"
```

### Task 13: Деплой edge + smoke

**Files:**
- Create: `deploy/web-push/deploy-edge-function.sh`

- [ ] **Step 1: Скрипт деплоя** (по образцу `deploy/nextcloud/deploy-edge-function.sh`)

```bash
#!/usr/bin/env bash
set -euo pipefail
SRC=/mnt/f/Сайт/redesign-v2-fresh/deploy/web-push/functions/web-push-notify
DST=/srv/supabase-src/docker/volumes/functions/web-push-notify
mkdir -p "$DST"
cp "$SRC/index.ts" "$DST/index.ts"
# config.json кладётся вручную (содержит VAPID private), не из git
[ -f "$DST/config.json" ] || echo "WARN: $DST/config.json отсутствует — положить с VAPID-ключами"
cd /srv/supabase-src/docker && docker compose restart edge-runtime
echo "deployed web-push-notify"
```

- [ ] **Step 2: Положить config.json на сервер** (вручную, с VAPID из Task 9), затем запустить деплой.
Run: `wsl -d Ubuntu -u root -- bash /mnt/f/Сайт/redesign-v2-fresh/deploy/web-push/deploy-edge-function.sh`
Expected: `deployed web-push-notify`, edge-runtime healthy.

- [ ] **Step 3: Smoke** — вызвать функцию с фейковым типом, проверить 200 (на тёплом worker; первый запрос после restart может дать WorkerRequestCancelled — повторить):
Run: `wsl -d Ubuntu -u root -- docker exec supabase-kong curl -s -X POST http://localhost:8000/functions/v1/web-push-notify -H "Authorization: Bearer $SERVICE_ROLE" -H "Content-Type: application/json" -d '{"type":"unknown"}'`
Expected: `{"ok":true,"note":"unknown type"}` — подтверждает, что `@negrel/webpush` импортировался и функция стартовала.

- [ ] **Step 4: Commit**
```
git ... add deploy/web-push/deploy-edge-function.sh
git ... commit -m "feat(push): деплой-скрипт edge web-push-notify"
```

---

## Фаза 5 — Интеграция точек событий

### Task 14: Обёртка sendPush + переключение точек

**Files:**
- Modify: `src/App.jsx`

- [ ] **Step 1: Обёртка** — переименовать/заменить `sendTelegramNotify` (App.jsx:644) на `sendPush`, тот же интерфейс, но `client.functions.invoke("web-push-notify", { body: {...} })`. Сохранить сигнатуру `(client, type, recipientId, data)` и `sendTaskNotify` (App.jsx:678) → invoke `web-push-notify`.

```js
async function sendPush(client, type, recipientId, data = {}) {
  try {
    await client.functions.invoke("web-push-notify", {
      body: { type, recipientId, initiatorId: data.initiatorId ?? null, ...data },
    })
  } catch (e) { console.warn("push notify failed", e) }
}
```

- [ ] **Step 2: Точки вызова** — заменить все `sendTelegramNotify(` на `sendPush(` (App.jsx ~2309, 2629, 4332, 4917) и `sendTaskNotify`/invoke на ~678. Семантика и аргументы те же (тип/получатель/данные).

- [ ] **Step 3: Новая точка project_published** — найти место, где проект переводится в стадию «Поиск исполнителя» (поиск по `"Поиск исполнителя"`, напр. ProjectForm save или смена stage). После успешного сохранения:
```js
sendPush(client, "project_published", null, { ownerId: profile.id, initiatorId: profile.id, projectId: p.id })
```

- [ ] **Step 4: Сборка + commit**
Run: `npm run build` → Expected: зелёная.
```
git ... add src/App.jsx
git ... commit -m "feat(push): переключить уведомления на web-push-notify + project_published"
```

---

## Фаза 6 — Deadline cron

### Task 15: pg_cron job для deadline

**Files:**
- Modify: `supabase/migrations/20260606_0003_pg_cron_deadline.sql` (добавить job к extensions из Task 4)

- [ ] **Step 1: Дописать cron job**

```sql
-- service_role key хранить в vault или app-настройке; здесь читаем из app.settings.
-- Грабля self-hosted (supabase#44907): URL только http://kong:8000, НЕ cloud/localhost.
select cron.schedule(
  'web-push-deadline',
  '0 9 * * *',  -- ежедневно 09:00
  $$
  select net.http_post(
    url := 'http://kong:8000/functions/v1/web-push-notify',
    headers := jsonb_build_object(
      'Content-Type','application/json',
      'Authorization','Bearer ' || current_setting('app.service_role_key', true)
    ),
    body := jsonb_build_object('type','deadline'),
    timeout_milliseconds := 30000
  );
  $$
);
```
Перед применением — задать `app.service_role_key` (ALTER DATABASE SET или через
vault). Если проще — захардкодить service_role в команде job (приватная БД, как в
официальных примерах), но предпочесть настройку/vault.

- [ ] **Step 2: Применить + проверить регистрацию**

Run: применить SQL через docker exec.
Run: `wsl -d Ubuntu -u root -- docker exec supabase-db psql -U postgres -d postgres -c "select jobname, schedule from cron.job where jobname='web-push-deadline'"`
Expected: одна строка с расписанием `0 9 * * *`.

- [ ] **Step 3: Ручной прогон + проверка ответа**

Run: вызвать `net.http_post` вручную (тот же SQL без cron.schedule), затем:
`... -c "select status, error_msg from net._http_response order by id desc limit 1"`
Expected: `status=200`, `error_msg` пуст (НЕ "Couldn't connect" — иначе URL неверный).

- [ ] **Step 4: Commit**
```
git ... add supabase/migrations/20260606_0003_pg_cron_deadline.sql
git ... commit -m "feat(push): pg_cron job web-push-deadline (09:00, kong:8000)"
```

---

## Фаза 7 — Деплой и приёмка

### Task 16: Полный деплой + E2E приёмка

- [ ] **Step 1: Деплой фронта**
Run: `npm run build` → `wsl -d Ubuntu -u root -- bash /mnt/f/Сайт/redesign-v2-fresh/deploy/nextcloud/deploy-web.sh`
Expected: `DEPLOYED`, прод отдаёт новый ассет + `/sw.js` + `/manifest.webmanifest` (curl 200).

- [ ] **Step 2: E2E — подписка**
На реальном устройстве: открыть прод → Настройки → вкл «Push на этом устройстве» → разрешить → проверить строку в `push_subscriptions` (psql).
Expected: одна строка с endpoint браузера под своим user_id.

- [ ] **Step 3: E2E — по каждому типу события**
Для task_assigned/status/created, project_taken, team_invite, comment, project_published: спровоцировать событие от другого пользователя → получатель с подпиской+флагом получает системное уведомление; снять флаг → не приходит; инициатор → не получает; клик открывает нужный объект.
Expected: для каждого типа — уведомление доставлено и фильтры работают.

- [ ] **Step 4: E2E — deadline + 410-очистка**
Ручной прогон `{type:'deadline'}` на задаче с близким сроком → push автору/исполнителю. Отписаться в браузере, оставив строку в БД, спровоцировать событие → edge получает 410 → строка удаляется (проверить psql).
Expected: deadline доставлен; протухшая подписка удалена.

- [ ] **Step 5: iOS** — установить PWA («На экран Домой»), включить push, спровоцировать событие.
Expected: уведомление приходит на iPhone; на не-установленном — виден install-баннер.

- [ ] **Step 6: Финальный commit + merge в main**
```
git ... add -A
git ... commit -m "feat(push): Web Push — приёмка пройдена"
# merge feature-ветки в main (--no-ff, Co-Authored-By) + push (обход прокси) — как делали для 6.4b
```

---

## Замечания по верификации (TDD-адаптация)

Браузерные/SW/push API не покрываются юнит-тестами в этом репо. Верификация —
проверяемыми наблюдаемыми результатами на каждом шаге: `npm run build` (зелёная
сборка ловит битые импорты/JSX), psql-проверки структуры БД, `verify-rls.sh`
(RLS_OK), smoke edge-функции (200 + импорт библиотеки), `net._http_response`
(cron достучался), и ручной E2E на реальных устройствах (push физически
доставлен). Каждый Task завершается явной проверкой перед commit.
