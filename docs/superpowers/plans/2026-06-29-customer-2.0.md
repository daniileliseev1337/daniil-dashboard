# Заказчик 2.0 — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Дать заказчику (роль `client`) активную роль в своих проектах — видеть/ставить задачи, комментировать, править ТЗ, принимать работу, создавать проекты-заявки с выбором пути подбора исполнителя — не пробивая изоляцию от внутренних данных; и закрыть существующую marketplace-утечку.

**Architecture:** Бэкенд — Postgres RLS + SECURITY DEFINER RPC поверх self-hosted Supabase; доступ заказчика гейтится централизованной функцией `can_access_project_comments` + узкими RPC для записи. Создание проектов — через отдельную сущность `project_requests`, материализуемую сотрудником. Фронт — монолит `src/App.jsx` (React 18 + Supabase JS), все запросы централизованы в блоке DATA OPERATIONS.

**Tech Stack:** Postgres 15 (self-hosted Supabase в WSL Docker), React 18.3, @supabase/supabase-js 2.45, Vite 5, Vitest (для JS-утилит). RLS-тесты — psql с имитацией JWT.

## Global Constraints

- **Дизайн-источник:** `docs/superpowers/specs/2026-06-28-customer-2.0-design.md` (v4). При расхождении плана и спеки — спека главнее, остановиться и уточнить.
- **Слой ролей:** «сотрудник» = роль `employee` в `user_roles`; «заказчик» = роль `client` (≡ привязка `clients.user_id`). Аккаунт может быть гибридом `{client,employee}`. НЕ трактовать `am_i_client()` как «значит не сотрудник».
- **owner_id материализованного проекта = сотрудник, НИКОГДА не заказчик** (`owner_id` — ключ RLS-доступа ко всей строке).
- **Изоляция заказчика:** заказчик НЕ должен получать прямой доступ к строке `projects` (`select('*')`) — только через `get_my_client_projects` (безопасная проекция). НЕ добавлять `is_project_client` в `projects_select`.
- **Среда:** репо `F:\Сайт\redesign-v2-fresh`; БД — контейнер `supabase-db` в WSL Docker.
- **Миграции:** файлы `supabase/migrations/20260629_NNNN_<name>.sql`, UTF-8 без BOM. Нумерация продолжает `20260626_0006`.
- **⚠ ГРАБЛЯ:** инструмент Edit падает на `src/App.jsx` (forced-fsync на F:). Правки `App.jsx` вносить через PowerShell `[IO.File]::WriteAllText($path,$text,(New-Object Text.UTF8Encoding $false))`. Новые `.sql`-файлы пишутся обычным Write.
- **Накат миграции:** `Get-Content -Raw <file> | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
- **RLS-тест (имитация пользователя X):**
  ```sql
  begin;
    set local role authenticated;
    set local request.jwt.claims = '{"sub":"<UUID>","role":"authenticated"}';
    -- проверяемый запрос
  rollback;
  ```
  `auth.uid()` читает `request.jwt.claims->>'sub'`. После проверки — `rollback` (тест не мутирует БД).

**Тестовые UUID (из данных БД на 2026-06-29):**
- Админ: `78e23498-b1a0-4a94-9a6a-c1596661a56c`
- Чистый сотрудник (employee, не client): `6a16f4df-62df-4b9c-bf4f-2298bf61b5ba`
- Гибрид (client+employee): `6d95d516-c46b-4121-b326-9e5e74a4486b`
- Чистый заказчик (client без employee): **создаётся в Task 0** → подставить его UUID в тесты ниже как `<CLIENT_UUID>`.

---

## File Structure

**Создаются (миграции):**
- `supabase/migrations/20260629_0001_is_employee.sql` — функция `is_employee()`.
- `supabase/migrations/20260629_0002_client_task_tz_access.sql` — `can_access_project_comments` + `is_project_client`; `propose_tz_version` + `is_project_client` (B-1).
- `supabase/migrations/20260629_0003_client_set_task_status.sql` — RPC приёмки (§4.2).
- `supabase/migrations/20260629_0004_assigned_to_guard.sql` — триггер валидации `assigned_to` (B-3).
- `supabase/migrations/20260629_0005_project_requests.sql` — таблица + RLS (§5.1/5.2).
- `supabase/migrations/20260629_0006_list_available_executors.sql` — RPC списка исполнителей (§5.4).
- `supabase/migrations/20260629_0007_project_request_rpcs.sql` — `create/accept/reject_project_request` (§5.3).
- `supabase/migrations/20260629_0008_marketplace_leak_fix.sql` — §7 (замена `is_approved`→`is_employee`).
- `supabase/migrations/20260629_0009_client_projects_task_count.sql` — `get_my_client_projects` + счётчик задач (§6).

**Модифицируется (фронт):**
- `src/App.jsx` — блок DATA OPERATIONS (новые обёртки), клиентский портал «Мои заказы» (создание заявки, задачи, приёмка), интерфейс сотрудника (входящие заявки).

**Тестовый сид:**
- Task 0 создаёт тестового чистого заказчика напрямую в БД (для проверки изоляции; без него все «client»-сценарии тестируются только на гибриде, который видит лишнее как employee).

---

## Task 0: Тестовый чистый заказчик (seed для проверки изоляции)

**Files:**
- Не создаёт файлов репо; вставляет тестовые данные в БД (откатываемо через cleanup-скрипт в конце фазы).

**Interfaces:**
- Produces: `<CLIENT_UUID>` — uuid аккаунта с ролью `client` БЕЗ `employee`, привязанного к новой записи `clients`. Используется в тестах Task 1–9.

- [ ] **Step 1: Создать auth-пользователя + profile + clients-запись + роль client**

Записать в `C:\Temp\seed_test_client.sql` (temp на C:, путь без кириллицы):
```sql
-- Тестовый чистый заказчик. Идемпотентно по фиксированному uuid.
do $$
declare v_uid uuid := '00000000-0000-4000-8000-00000000c111';
begin
  insert into auth.users (id, email, role, aud, instance_id, created_at, updated_at)
  values (v_uid, 'test-client@example.com', 'authenticated', 'authenticated',
          '00000000-0000-0000-0000-000000000000', now(), now())
  on conflict (id) do nothing;

  insert into public.profiles (id, email, name, role, approved)
  values (v_uid, 'test-client@example.com', 'Тест Заказчик', 'user', true)
  on conflict (id) do update set approved = true;

  insert into public.clients (owner_id, name, user_id)
  values ('78e23498-b1a0-4a94-9a6a-c1596661a56c', 'ТестКлиент ООО', v_uid)
  on conflict do nothing;

  insert into public.user_roles (user_id, role) values (v_uid, 'client')
  on conflict do nothing;
end $$;

select p.id, p.approved, array_agg(ur.role) roles, c.id as client_id
from public.profiles p
left join public.user_roles ur on ur.user_id = p.id
left join public.clients c on c.user_id = p.id
where p.id = '00000000-0000-4000-8000-00000000c111'
group by p.id, p.approved, c.id;
```

- [ ] **Step 2: Применить и проверить**

Run: `Get-Content -Raw C:\Temp\seed_test_client.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: одна строка — `approved=t`, `roles={client}`, `client_id` не null.
Зафиксировать `<CLIENT_UUID>` = `00000000-0000-4000-8000-00000000c111` для последующих тестов.

- [ ] **Step 3: Дать этому заказчику один проект (для тестов видимости задач/приёмки)**

В `C:\Temp\seed_test_client.sql` дописать и повторно применить (или отдельным запросом):
```sql
insert into public.projects (owner_id, client_id, name, stage, visibility)
select '6a16f4df-62df-4b9c-bf4f-2298bf61b5ba',
       (select id from public.clients where user_id = '00000000-0000-4000-8000-00000000c111' limit 1),
       'Тестовый заказ клиента', 'В работе', 'private'
where not exists (select 1 from public.projects where name = 'Тестовый заказ клиента');
select id, owner_id, client_id from public.projects where name = 'Тестовый заказ клиента';
```
Expected: проект создан, `owner_id`=сотрудник, `client_id`=тестовый клиент. Зафиксировать `<CLIENT_PROJECT_UUID>`.

**Нет коммита** (это тестовые данные БД, не код). Cleanup — в конце фазы.

---

## Task 1: Функция `is_employee()`

**Files:**
- Create: `supabase/migrations/20260629_0001_is_employee.sql`

**Interfaces:**
- Produces: `public.is_employee() returns boolean` — `true` для одобренного аккаунта с ролью `employee`. Используют Tasks 5,6,7,8.

- [ ] **Step 1: Написать тест (psql) — фиксируем ожидаемое поведение ДО функции**

Записать `C:\Temp\test_is_employee.sql`:
```sql
-- Функции ещё нет — этот запрос должен УПАСТЬ (function does not exist)
begin;
  set local role authenticated;
  set local request.jwt.claims = '{"sub":"6a16f4df-62df-4b9c-bf4f-2298bf61b5ba","role":"authenticated"}';
  select public.is_employee() as should_be_true;
rollback;
```

- [ ] **Step 2: Запустить тест — убедиться, что падает**

Run: `Get-Content -Raw C:\Temp\test_is_employee.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres`
Expected: FAIL — `ERROR: function public.is_employee() does not exist`.

- [ ] **Step 3: Написать миграцию**

Создать `supabase/migrations/20260629_0001_is_employee.sql`:
```sql
-- 20260629_0001: is_employee() — одобренный аккаунт с ролью employee в user_roles.
-- НЕ "approved AND NOT am_i_client": аккаунт бывает гибридом {client,employee} — он сотрудник.
create or replace function public.is_employee()
returns boolean language sql stable security definer
set search_path = public, pg_temp as $$
  select public.is_approved()
     and exists (select 1 from public.user_roles
                 where user_id = auth.uid() and role = 'employee');
$$;
grant execute on function public.is_employee() to authenticated;
```

- [ ] **Step 4: Применить миграцию**

Run: `Get-Content -Raw supabase/migrations/20260629_0001_is_employee.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: `CREATE FUNCTION` + `GRANT`.

- [ ] **Step 5: Прогнать матрицу проверок**

Записать `C:\Temp\test_is_employee2.sql` и запустить:
```sql
-- чистый сотрудник → true
begin; set local role authenticated; set local request.jwt.claims='{"sub":"6a16f4df-62df-4b9c-bf4f-2298bf61b5ba","role":"authenticated"}';
  select 'employee' k, public.is_employee() v; rollback;
-- гибрид client+employee → true (он сотрудник)
begin; set local role authenticated; set local request.jwt.claims='{"sub":"6d95d516-c46b-4121-b326-9e5e74a4486b","role":"authenticated"}';
  select 'hybrid' k, public.is_employee() v; rollback;
-- чистый заказчик → false
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select 'pure_client' k, public.is_employee() v; rollback;
```
Expected: `employee=t`, `hybrid=t`, `pure_client=f`.

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/20260629_0001_is_employee.sql
git commit -m "feat(db): is_employee() на роли employee из user_roles"
```

---

## Task 2: Доступ заказчика к задачам/комментариям/ТЗ (§4.1 + B-1)

**Files:**
- Create: `supabase/migrations/20260629_0002_client_task_tz_access.sql`

**Interfaces:**
- Consumes: `is_project_client(uuid)` (существует), `can_access_project_comments(uuid)` (существует), `propose_tz_version(uuid,text)` (существует).
- Produces: расширенные версии тех же функций — заказчик получает SELECT/INSERT задач/комментариев своих проектов и право предлагать ТЗ.

- [ ] **Step 1: Тест — заказчик пока НЕ видит задачи своего проекта**

`C:\Temp\test_client_tasks.sql`:
```sql
-- Заказчик читает задачи своего проекта через RLS (tasks_select → can_access_project_comments).
-- До правки can_access_project_comments не знает про is_project_client → 0 строк (если задачи есть).
-- Сначала вставим задачу в проект клиента от имени сотрудника-владельца:
insert into public.project_tasks (project_id, author_id, title, description, status)
select '<CLIENT_PROJECT_UUID>', '6a16f4df-62df-4b9c-bf4f-2298bf61b5ba',
       'Проверка ТЗ от сотрудника', 'Первичное ТЗ', 'На проверке'
where not exists (select 1 from public.project_tasks where title='Проверка ТЗ от сотрудника');

begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select count(*) as visible_tasks from public.project_tasks where project_id='<CLIENT_PROJECT_UUID>';
rollback;
```
Run: `Get-Content -Raw C:\Temp\test_client_tasks.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres`
Expected (ДО правки): `visible_tasks = 0` (заказчик не видит). Зафиксировать `<CLIENT_TASK_UUID>` отдельным `select id from project_tasks where title='Проверка ТЗ от сотрудника';`

- [ ] **Step 2: Написать миграцию**

`supabase/migrations/20260629_0002_client_task_tz_access.sql`:
```sql
-- 20260629_0002: доступ заказчика к задачам/комментариям/ТЗ своих проектов.
-- (§4.1) Централизованный гейт + (B-1) право заказчика предлагать ТЗ к задаче сотрудника.

-- 1. can_access_project_comments: добавить ветку is_project_client.
create or replace function public.can_access_project_comments(p_project_id uuid)
returns boolean language sql stable security definer
set search_path to 'public', 'pg_temp' as $$
  select exists (
    select 1 from public.projects
    where id = p_project_id
      and (
        owner_id = auth.uid()
        or public.is_admin()
        or (visibility = 'team' and public.is_project_member(p_project_id))
        or (visibility = 'marketplace' and public.is_approved())
        or public.is_project_client(p_project_id)            -- НОВОЕ: заказчик своего проекта
      )
  );
$$;

-- 2. propose_tz_version: расширить гейт правом заказчика (B-1).
--    Полное переопределение (формат сохранён из живой БД, добавлена ветка is_project_client).
create or replace function public.propose_tz_version(p_task_id uuid, p_content text)
returns task_tz_versions language plpgsql security definer as $function$
declare
  t public.project_tasks%ROWTYPE;
  v_caller uuid := auth.uid();
  v_is_party boolean; v_can_edit boolean;
  v_next_no int; v_status text; v_resolved uuid; v_resolved_at timestamptz;
  v_row public.task_tz_versions%ROWTYPE;
begin
  if not public.is_approved() then raise exception 'not_approved'; end if;
  select * into t from public.project_tasks where id = p_task_id;
  if not found then raise exception 'task_not_found'; end if;
  if not public.can_access_task(p_task_id) then raise exception 'access_denied'; end if;

  v_is_party := (t.author_id = v_caller or t.assigned_to = v_caller);
  v_can_edit := public.is_admin()
                or (t.project_id is not null and public.is_project_editor(t.project_id))
                or (t.project_id is not null and public.is_project_client(t.project_id)); -- B-1

  if not (v_is_party or v_can_edit) then raise exception 'forbidden'; end if;

  if exists (select 1 from public.task_tz_versions where task_id = p_task_id and status = 'pending') then
    raise exception 'tz_pending_exists';
  end if;

  if t.assigned_to is not null and v_is_party then
    v_status := 'pending'; v_resolved := null; v_resolved_at := null;
  else
    v_status := 'approved'; v_resolved := v_caller; v_resolved_at := now();
  end if;

  select coalesce(max(version_no),0)+1 into v_next_no from public.task_tz_versions where task_id = p_task_id;
  insert into public.task_tz_versions (task_id, version_no, content, status, proposed_by, resolved_by, resolved_at)
  values (p_task_id, v_next_no, p_content, v_status, v_caller, v_resolved, v_resolved_at)
  returning * into v_row;
  if v_status = 'approved' then
    update public.project_tasks set description = p_content where id = p_task_id;
  end if;
  return v_row;
end $function$;
```

- [ ] **Step 3: Применить миграцию**

Run: `Get-Content -Raw supabase/migrations/20260629_0002_client_task_tz_access.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: два `CREATE FUNCTION`.

- [ ] **Step 4: Тест — заказчик теперь видит задачи, пишет комментарий, предлагает ТЗ**

`C:\Temp\test_client_tasks2.sql`:
```sql
-- видит задачи
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select count(*) tasks_visible from public.project_tasks where project_id='<CLIENT_PROJECT_UUID>';
rollback;
-- предлагает ТЗ к задаче, поставленной СОТРУДНИКОМ (B-1) — должно пройти, вернуть строку версии
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select (public.propose_tz_version('<CLIENT_TASK_UUID>','Уточнённое ТЗ от заказчика')).version_no as new_tz_version;
rollback;
```
Expected: `tasks_visible >= 1`; `new_tz_version = 2` (без exception `forbidden`).

- [ ] **Step 5: Регресс — чужой заказчик НЕ видит этот проект**

```sql
begin; set local role authenticated; set local request.jwt.claims='{"sub":"6d95d516-c46b-4121-b326-9e5e74a4486b","role":"authenticated"}';
  -- гибрид НЕ привязан к этому client_id → как заказчик не видит; (как сотрудник видит только если member/owner)
  select count(*) as hybrid_sees from public.project_tasks where project_id='<CLIENT_PROJECT_UUID>';
rollback;
```
Expected: `hybrid_sees = 0` (проект `private`, гибрид не owner/member/его-клиент).

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/20260629_0002_client_task_tz_access.sql
git commit -m "feat(db): доступ заказчика к задачам/комментариям/ТЗ своих проектов (§4.1, B-1)"
```

---

## Task 3: RPC приёмки `client_set_task_status` (§4.2)

**Files:**
- Create: `supabase/migrations/20260629_0003_client_set_task_status.sql`

**Interfaces:**
- Produces: `public.client_set_task_status(p_task_id uuid, p_status text) returns void`. Фронт-обёртка — Task 10.

- [ ] **Step 1: Тест — заказчик пока не может сменить статус чужой задачи напрямую**

`C:\Temp\test_accept.sql`:
```sql
-- Прямой UPDATE заказчиком блокируется tasks_update USING (он не author/assignee/admin/editor)
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  update public.project_tasks set status='Готово' where id='<CLIENT_TASK_UUID>';
  select status from public.project_tasks where id='<CLIENT_TASK_UUID>';  -- останется прежним
rollback;
```
Run и Expected: статус НЕ изменился (`На проверке`) — RLS не пустил UPDATE (0 rows updated).

- [ ] **Step 2: Написать миграцию**

`supabase/migrations/20260629_0003_client_set_task_status.sql`:
```sql
-- 20260629_0003: узкий RPC приёмки. Заказчик двигает только статус приёмки своих задач.
create or replace function public.client_set_task_status(p_task_id uuid, p_status text)
returns void language plpgsql security definer
set search_path = public, pg_temp as $$
declare
  t public.project_tasks%ROWTYPE;
begin
  select * into t from public.project_tasks where id = p_task_id;
  if not found then raise exception 'task_not_found'; end if;
  if t.project_id is null or not public.is_project_client(t.project_id) then
    raise exception 'forbidden';
  end if;
  -- разрешённые переходы приёмки
  if not (
       (t.status = 'На проверке' and p_status in ('Готово','В работе'))
    or (t.status = 'Готово'      and p_status = 'В работе')
  ) then
    raise exception 'illegal_transition: % -> %', t.status, p_status;
  end if;
  update public.project_tasks set status = p_status where id = p_task_id;
end $$;
grant execute on function public.client_set_task_status(uuid, text) to authenticated;
```

- [ ] **Step 3: Применить миграцию**

Run: `Get-Content -Raw supabase/migrations/20260629_0003_client_set_task_status.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: `CREATE FUNCTION` + `GRANT`.

- [ ] **Step 4: Тест — приёмка и запреты**

`C:\Temp\test_accept2.sql`:
```sql
-- легальный переход На проверке → Готово
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  perform public.client_set_task_status('<CLIENT_TASK_UUID>','Готово');
  select status from public.project_tasks where id='<CLIENT_TASK_UUID>';
rollback;
-- нелегальный переход Новая → В работе (должен упасть). Подготовить задачу в статусе Новая:
-- (берём любую/создаём) — здесь проверяем на исключение
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  perform public.client_set_task_status('<CLIENT_TASK_UUID_NEW_STATUS>','В работе');
rollback;
```
Expected: первый блок → `Готово`; второй → `ERROR: illegal_transition`. (Для второго заранее создать задачу в статусе `Новая` в проекте клиента и подставить её uuid.)

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260629_0003_client_set_task_status.sql
git commit -m "feat(db): client_set_task_status — приёмка задач заказчиком (§4.2)"
```

---

## Task 4: Валидация `assigned_to` при создании задачи заказчиком (B-3)

**Files:**
- Create: `supabase/migrations/20260629_0004_assigned_to_guard.sql`

**Interfaces:**
- Produces: BEFORE-триггер `trg_validate_assigned_to` на `project_tasks`. Заказчик не может назначить задачу uid вне `executors` проекта.

**Замечание о scope:** триггер применяется только когда автор задачи — заказчик (не сотрудник/админ), чтобы не ломать существующие сотруднические сценарии.

- [ ] **Step 1: Тест — заказчик пока может назначить произвольный uid**

`C:\Temp\test_assignee.sql`:
```sql
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  insert into public.project_tasks (project_id, author_id, title, assigned_to, status)
  values ('<CLIENT_PROJECT_UUID>','00000000-0000-4000-8000-00000000c111',
          'Задача с чужим исполнителем','78e23498-b1a0-4a94-9a6a-c1596661a56c','Новая')
  returning assigned_to;
rollback;
```
Run и Expected (ДО триггера): вставка проходит, `assigned_to` = произвольный admin uid (нежелательно).

- [ ] **Step 2: Написать миграцию**

`supabase/migrations/20260629_0004_assigned_to_guard.sql`:
```sql
-- 20260629_0004: заказчик-автор не может назначить assigned_to вне executors проекта (B-3).
create or replace function public.validate_client_task_assignee()
returns trigger language plpgsql security definer
set search_path = public, pg_temp as $$
begin
  -- срабатывает только если автор — заказчик данного проекта и НЕ сотрудник
  if NEW.project_id is not null
     and public.is_project_client(NEW.project_id)
     and not public.is_employee()
     and NEW.assigned_to is not null then
    if not exists (
      select 1 from public.projects p,
             jsonb_array_elements(coalesce(p.executors,'[]'::jsonb)) e
      where p.id = NEW.project_id
        and (e->>'userId') = NEW.assigned_to::text
    ) then
      raise exception 'assignee_not_in_executors';
    end if;
  end if;
  return NEW;
end $$;

drop trigger if exists trg_validate_assigned_to on public.project_tasks;
create trigger trg_validate_assigned_to
  before insert or update of assigned_to on public.project_tasks
  for each row execute function public.validate_client_task_assignee();
```

- [ ] **Step 3: Применить миграцию**

Run: `Get-Content -Raw supabase/migrations/20260629_0004_assigned_to_guard.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: `CREATE FUNCTION` + `CREATE TRIGGER`.

- [ ] **Step 4: Тест — заказчик не может назначить вне executors; null проходит**

```sql
-- чужой uid → исключение
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  insert into public.project_tasks (project_id, author_id, title, assigned_to, status)
  values ('<CLIENT_PROJECT_UUID>','00000000-0000-4000-8000-00000000c111','x','78e23498-b1a0-4a94-9a6a-c1596661a56c','Новая');
rollback;
-- null (в пул) → проходит
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  insert into public.project_tasks (project_id, author_id, title, assigned_to, status)
  values ('<CLIENT_PROJECT_UUID>','00000000-0000-4000-8000-00000000c111','y',null,'Новая') returning id;
rollback;
-- сотрудник по-прежнему может назначить кого угодно (регресс) → проходит
begin; set local role authenticated; set local request.jwt.claims='{"sub":"6a16f4df-62df-4b9c-bf4f-2298bf61b5ba","role":"authenticated"}';
  insert into public.project_tasks (project_id, author_id, title, assigned_to, status)
  values ('<CLIENT_PROJECT_UUID>','6a16f4df-62df-4b9c-bf4f-2298bf61b5ba','z','78e23498-b1a0-4a94-9a6a-c1596661a56c','Новая') returning id;
rollback;
```
Expected: блок1 → `ERROR: assignee_not_in_executors`; блок2 → проходит; блок3 → проходит.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260629_0004_assigned_to_guard.sql
git commit -m "feat(db): валидация assigned_to для задач заказчика (B-3)"
```

---

## Task 5: Таблица `project_requests` + RLS (§5.1, §5.2)

**Files:**
- Create: `supabase/migrations/20260629_0005_project_requests.sql`

**Interfaces:**
- Consumes: `is_employee()` (Task 1), `am_i_client()` (существует).
- Produces: таблица `public.project_requests` с колонками из §5.1; политики SELECT/INSERT.

- [ ] **Step 1: Написать миграцию**

`supabase/migrations/20260629_0005_project_requests.sql`:
```sql
-- 20260629_0005: заявки на проекты от заказчика (§5.1/5.2).
create table if not exists public.project_requests (
  id uuid primary key default gen_random_uuid(),
  client_id uuid not null references public.clients(id),
  created_by uuid not null references public.profiles(id),
  name text not null,
  description text,
  desired_deadline date,
  mode text not null default 'quick' check (mode in ('quick','detailed')),
  assignment_mode text not null default 'marketplace' check (assignment_mode in ('marketplace','assignee')),
  desired_executor_id uuid references public.profiles(id),
  status text not null default 'Новая' check (status in ('Новая','Принята','Отклонена')),
  accepted_project_id uuid references public.projects(id),
  created_at timestamptz not null default now()
);

alter table public.project_requests enable row level security;

-- Хелпер (m-2): заказчик НЕ видит свою запись clients под RLS (clients_select = owner/admin),
-- поэтому прямой exists(clients) в WITH CHECK вернёт false. Выносим в SECURITY DEFINER.
create or replace function public.is_my_client_record(p_client_id uuid)
returns boolean language sql stable security definer
set search_path = public, pg_temp as $$
  select exists (select 1 from public.clients where id = p_client_id and user_id = auth.uid());
$$;
grant execute on function public.is_my_client_record(uuid) to authenticated;

-- SELECT: создатель, сотрудник (is_employee), админ.
create policy "preq_select" on public.project_requests for select to authenticated
  using (created_by = auth.uid() or public.is_employee() or public.is_admin());

-- INSERT: заказчик создаёт от своего имени, client_id принадлежит ему (через SECURITY DEFINER хелпер).
create policy "preq_insert" on public.project_requests for insert to authenticated
  with check (
    public.am_i_client()
    and created_by = auth.uid()
    and public.is_my_client_record(client_id)
  );

-- UPDATE/DELETE: только через RPC (SECURITY DEFINER) или админ.
create policy "preq_update_admin" on public.project_requests for update to authenticated
  using (public.is_admin()) with check (public.is_admin());
create policy "preq_delete_admin" on public.project_requests for delete to authenticated
  using (public.is_admin());

create index if not exists idx_preq_created_by on public.project_requests(created_by);
create index if not exists idx_preq_status on public.project_requests(status);
```

- [ ] **Step 2: Применить миграцию**

Run: `Get-Content -Raw supabase/migrations/20260629_0005_project_requests.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: `CREATE TABLE`, `ALTER TABLE`, `CREATE POLICY`×4, `CREATE INDEX`×2.

- [ ] **Step 3: Тест — заказчик вставляет свою заявку, не может вставить с чужим client_id**

`C:\Temp\test_preq.sql`:
```sql
-- своя заявка → проходит
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  insert into public.project_requests (client_id, created_by, name, assignment_mode)
  values ((select id from public.clients where user_id='00000000-0000-4000-8000-00000000c111' limit 1),
          '00000000-0000-4000-8000-00000000c111','Заявка тест','marketplace') returning id;
rollback;
-- чужой client_id → блок (нет clients с этим user_id)
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  insert into public.project_requests (client_id, created_by, name)
  values ('00000000-0000-0000-0000-000000000999','00000000-0000-4000-8000-00000000c111','Чужая');
rollback;
```
Expected: блок1 → INSERT проходит; блок2 → `new row violates row-level security policy`.

- [ ] **Step 4: Тест — сотрудник видит заявки, чужой заказчик не видит чужую**

```sql
-- сначала вставим заявку клиента (вне транзакции, чтобы видна другим)
insert into public.project_requests (client_id, created_by, name, assignment_mode)
select (select id from public.clients where user_id='00000000-0000-4000-8000-00000000c111' limit 1),
       '00000000-0000-4000-8000-00000000c111','Видимость-тест','marketplace'
where not exists (select 1 from public.project_requests where name='Видимость-тест');
-- сотрудник видит
begin; set local role authenticated; set local request.jwt.claims='{"sub":"6a16f4df-62df-4b9c-bf4f-2298bf61b5ba","role":"authenticated"}';
  select count(*) emp_sees from public.project_requests where name='Видимость-тест'; rollback;
```
Expected: `emp_sees = 1`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260629_0005_project_requests.sql
git commit -m "feat(db): таблица project_requests + RLS (§5.1, §5.2)"
```

---

## Task 6: RPC `list_available_executors` (§5.4)

**Files:**
- Create: `supabase/migrations/20260629_0006_list_available_executors.sql`

**Interfaces:**
- Produces: `public.list_available_executors() returns table(id uuid, name text, position text)`. Используется фронтом (Task 11) и `create_project_request` для валидации (Task 7).

- [ ] **Step 1: Написать миграцию**

`supabase/migrations/20260629_0006_list_available_executors.sql`:
```sql
-- 20260629_0006: список исполнителей для заказчика (узкая проекция, §5.4).
create or replace function public.list_available_executors()
returns table(id uuid, name text, "position" text)
language sql stable security definer set search_path = public, pg_temp as $$
  select p.id, p.name, p.position
  from public.profiles p
  join public.user_roles ur on ur.user_id = p.id and ur.role = 'employee'
  where p.approved = true and p.id <> auth.uid()
  order by p.name;
$$;
grant execute on function public.list_available_executors() to authenticated;
```

- [ ] **Step 2: Применить миграцию**

Run: `Get-Content -Raw supabase/migrations/20260629_0006_list_available_executors.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: `CREATE FUNCTION` + `GRANT`.

- [ ] **Step 3: Тест — заказчик видит сотрудников, без email/role, не видит себя**

```sql
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select count(*) n, bool_and(id <> '00000000-0000-4000-8000-00000000c111') no_self
  from public.list_available_executors();
rollback;
```
Expected: `n = 6` (все employee), `no_self = t`. Колонки — только id/name/position (по сигнатуре).

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260629_0006_list_available_executors.sql
git commit -m "feat(db): list_available_executors — узкий список исполнителей для заказчика (§5.4)"
```

---

## Task 7: RPC создания/приёма/отклонения заявки (§5.3)

**Files:**
- Create: `supabase/migrations/20260629_0007_project_request_rpcs.sql`

**Interfaces:**
- Consumes: `is_employee()`, `list_available_executors()`, `am_i_client()`, таблица `project_requests`.
- Produces:
  - `create_project_request(p_name text, p_description text, p_deadline date, p_mode text, p_assignment_mode text, p_desired_executor_id uuid default null, p_client_id uuid default null) returns uuid`
  - `accept_project_request(p_request_id uuid) returns uuid` (id созданного проекта)
  - `reject_project_request(p_request_id uuid, p_reason text default null) returns void`

- [ ] **Step 1: Написать миграцию**

`supabase/migrations/20260629_0007_project_request_rpcs.sql`:
```sql
-- 20260629_0007: RPC жизненного цикла заявки (§5.3).

-- create_project_request: заказчик создаёт заявку; валидирует executor; уведомляет сотрудников.
create or replace function public.create_project_request(
  p_name text, p_description text, p_deadline date, p_mode text,
  p_assignment_mode text, p_desired_executor_id uuid default null, p_client_id uuid default null
) returns uuid language plpgsql security definer
set search_path = public, pg_temp as $$
declare
  v_client uuid;
  v_req uuid;
begin
  if not public.am_i_client() then raise exception 'not_client'; end if;
  if p_mode not in ('quick','detailed') then raise exception 'bad_mode'; end if;
  if p_assignment_mode not in ('marketplace','assignee') then raise exception 'bad_assignment_mode'; end if;

  -- client_id: переданный (если принадлежит заказчику) или единственная запись заказчика
  v_client := coalesce(
    (select c.id from public.clients c where c.id = p_client_id and c.user_id = auth.uid()),
    (select c.id from public.clients c where c.user_id = auth.uid() order by c.created_at limit 1)
  );
  if v_client is null then raise exception 'no_client_record'; end if;

  -- assignee: исполнитель обязателен и должен быть в списке доступных
  if p_assignment_mode = 'assignee' then
    if p_desired_executor_id is null
       or not exists (select 1 from public.list_available_executors() e where e.id = p_desired_executor_id) then
      raise exception 'invalid_executor';
    end if;
  end if;

  insert into public.project_requests
    (client_id, created_by, name, description, desired_deadline, mode, assignment_mode, desired_executor_id)
  values (v_client, auth.uid(), p_name, p_description, p_deadline, p_mode, p_assignment_mode,
          case when p_assignment_mode='assignee' then p_desired_executor_id else null end)
  returning id into v_req;

  -- in-app уведомление сотрудникам. Схема notifications: user_id/type/title/body/url (все NOT NULL).
  insert into public.notifications (user_id, type, title, body, url)
  select ur.user_id, 'project_request', 'Новая заявка от заказчика', p_name, '/requests'
  from public.user_roles ur where ur.role = 'employee';

  return v_req;
end $$;
grant execute on function public.create_project_request(text,text,date,text,text,uuid,uuid) to authenticated;

-- accept_project_request: сотрудник материализует проект из заявки (один клик).
create or replace function public.accept_project_request(p_request_id uuid)
returns uuid language plpgsql security definer
set search_path = public, pg_temp as $$
declare
  r public.project_requests%ROWTYPE;
  v_pid uuid;
  v_exec_name text;
begin
  if not (public.is_employee() or public.is_admin()) then raise exception 'forbidden'; end if;
  select * into r from public.project_requests where id = p_request_id for update;
  if not found then raise exception 'request_not_found'; end if;
  if r.status <> 'Новая' then raise exception 'already_processed'; end if;

  if r.assignment_mode = 'marketplace' then
    insert into public.projects (owner_id, client_id, name, stage, visibility)
    values (auth.uid(), r.client_id, r.name, 'Поиск исполнителя', 'marketplace')
    returning id into v_pid;
  else
    -- assignee: проект + исполнитель в команду и executors (паттерн take_project, но НЕ вызов).
    insert into public.projects (owner_id, client_id, name, stage, visibility)
    values (auth.uid(), r.client_id, r.name, 'В работе', 'team')
    returning id into v_pid;

    select coalesce(nullif(name,''), email) into v_exec_name
    from public.profiles where id = r.desired_executor_id;

    update public.projects
    set executors = jsonb_build_array(jsonb_build_object('name', coalesce(v_exec_name,''),
                                                         'userId', r.desired_executor_id::text)),
        executor  = coalesce(v_exec_name,'')
    where id = v_pid;

    insert into public.project_members (project_id, user_id, role)
    values (v_pid, r.desired_executor_id, 'editor')
    on conflict (project_id, user_id) do update set role = 'editor';
  end if;

  update public.project_requests
  set status = 'Принята', accepted_project_id = v_pid where id = p_request_id;

  -- уведомить заказчика
  insert into public.notifications (user_id, type, title, body, url)
  values (r.created_by, 'project_request', 'Заявка принята', r.name, '/orders');

  return v_pid;
end $$;
grant execute on function public.accept_project_request(uuid) to authenticated;

-- reject_project_request
create or replace function public.reject_project_request(p_request_id uuid, p_reason text default null)
returns void language plpgsql security definer
set search_path = public, pg_temp as $$
declare r public.project_requests%ROWTYPE;
begin
  if not (public.is_employee() or public.is_admin()) then raise exception 'forbidden'; end if;
  select * into r from public.project_requests where id = p_request_id for update;
  if not found then raise exception 'request_not_found'; end if;
  if r.status <> 'Новая' then raise exception 'already_processed'; end if;
  update public.project_requests set status = 'Отклонена' where id = p_request_id;
  insert into public.notifications (user_id, type, title, body, url)
  values (r.created_by, 'project_request', 'Заявка отклонена', coalesce(p_reason, r.name), '/orders');
end $$;
grant execute on function public.reject_project_request(uuid, text) to authenticated;
```

**Схема `notifications` сверена (2026-06-29):** `user_id, type, title, body, url` — все NOT NULL,
`url` default `'/'`. INSERT-блоки выше уже приведены под неё. RLS notifications не имеет
INSERT-политики, но RPC `SECURITY DEFINER` от owner (postgres) обходит RLS (table-owner bypass) —
вставка уведомлений работает.

- [ ] **Step 2: Применить миграцию**

Run: `Get-Content -Raw supabase/migrations/20260629_0007_project_request_rpcs.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: три `CREATE FUNCTION` + три `GRANT`.

- [ ] **Step 3: Тест — полный цикл marketplace и assignee**

`C:\Temp\test_rpc_cycle.sql`:
```sql
-- marketplace: заказчик создаёт, сотрудник принимает → проект в Поиск исполнителя
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select public.create_project_request('MP заявка','описание',null,'quick','marketplace') as req_id; 
rollback;
```
Затем материализация (две роли — нельзя в одной транзакции с rollback, проверять последовательно, фиксируя id). Полный сценарий (без rollback, с очисткой):
```sql
-- 1) создать заявку (как заказчик)
set role authenticated; set request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
select public.create_project_request('MP заявка e2e',null,null,'quick','marketplace') as req_id \gset
reset role; reset request.jwt.claims;
-- 2) принять (как сотрудник)
set role authenticated; set request.jwt.claims='{"sub":"6a16f4df-62df-4b9c-bf4f-2298bf61b5ba","role":"authenticated"}';
select public.accept_project_request(:'req_id') as new_pid \gset
reset role; reset request.jwt.claims;
-- 3) проверить проект
select id, stage, visibility, owner_id from public.projects where id = :'new_pid';
-- 4) проверить статус заявки
select status, accepted_project_id from public.project_requests where id = :'req_id';
```
Expected: проект `stage='Поиск исполнителя'`, `visibility='marketplace'`, `owner_id`=сотрудник; заявка `status='Принята'`.

- [ ] **Step 4: Тест — assignee-цикл и валидация executor**

```sql
-- невалидный executor → исключение
set role authenticated; set request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
select public.create_project_request('bad','x',null,'quick','assignee','00000000-0000-0000-0000-000000000999');
reset role; reset request.jwt.claims;
-- валидный executor (берём реального сотрудника)
set role authenticated; set request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
select public.create_project_request('assignee e2e',null,null,'detailed','assignee','6a16f4df-62df-4b9c-bf4f-2298bf61b5ba') as req2 \gset
reset role; reset request.jwt.claims;
set role authenticated; set request.jwt.claims='{"sub":"78e23498-b1a0-4a94-9a6a-c1596661a56c","role":"authenticated"}';
select public.accept_project_request(:'req2') as pid2 \gset
reset role; reset request.jwt.claims;
select p.stage, p.executors, (select count(*) from public.project_members where project_id=p.id and user_id='6a16f4df-62df-4b9c-bf4f-2298bf61b5ba' and role='editor') as in_team
from public.projects p where p.id=:'pid2';
```
Expected: первый вызов → `ERROR: invalid_executor`; assignee-проект → `stage='В работе'`, `executors` содержит `userId` сотрудника, `in_team=1`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260629_0007_project_request_rpcs.sql
git commit -m "feat(db): create/accept/reject_project_request — цикл заявки (§5.3)"
```

---

## Task 8: Закрытие marketplace-утечки (§7)

**Files:**
- Create: `supabase/migrations/20260629_0008_marketplace_leak_fix.sql`

**Interfaces:**
- Modifies: `projects_select` (RLS) и `can_access_project_comments` — замена `is_approved()` на `is_employee()` в marketplace-ветке.

**⚠ Зависимость:** выполнять ТОЛЬКО после Task 9 (расширение `get_my_client_projects`), иначе заказчик временно теряет видимость своего marketplace-проекта между Task 8 и Task 9. **Рекомендуемый порядок: сделать Task 9 перед Task 8.** (Нумерация файлов не диктует порядок применения — применять 0009 до 0008.)

- [ ] **Step 1: Тест — ДО правки чистый заказчик видит чужой marketplace-проект прямым запросом**

Сначала создать чужой marketplace-проект (владелец — сотрудник, без привязки к тест-клиенту):
```sql
insert into public.projects (owner_id, name, stage, visibility)
select '6a16f4df-62df-4b9c-bf4f-2298bf61b5ba','Чужой маркет-проект','Поиск исполнителя','marketplace'
where not exists (select 1 from public.projects where name='Чужой маркет-проект');
```
```sql
-- чистый заказчик прямым запросом
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select count(*) leak from public.projects where name='Чужой маркет-проект';
rollback;
```
Run и Expected (ДО): `leak = 1` — утечка подтверждена.

- [ ] **Step 2: Написать миграцию**

`supabase/migrations/20260629_0008_marketplace_leak_fix.sql`:
```sql
-- 20260629_0008: §7 — marketplace-ветки видят только СОТРУДНИКИ (is_employee), не любой approved.
-- Закрывает утечку: чистый заказчик больше не видит чужие marketplace-проекты прямым запросом.
-- Свой проект заказчик видит через get_my_client_projects (Task 9), НЕ через projects_select.

-- 1. projects_select
drop policy if exists "projects_select" on public.projects;
create policy "projects_select" on public.projects for select to authenticated
  using (
    owner_id = auth.uid()
    or public.is_admin()
    or (visibility = 'team' and public.is_project_member(id))
    or (visibility = 'marketplace' and public.is_employee())   -- было is_approved()
  );

-- 2. can_access_project_comments — marketplace-ветка на is_employee (ветка is_project_client из 0002 сохранена)
create or replace function public.can_access_project_comments(p_project_id uuid)
returns boolean language sql stable security definer
set search_path to 'public', 'pg_temp' as $$
  select exists (
    select 1 from public.projects
    where id = p_project_id
      and (
        owner_id = auth.uid()
        or public.is_admin()
        or (visibility = 'team' and public.is_project_member(p_project_id))
        or (visibility = 'marketplace' and public.is_employee())  -- было is_approved()
        or public.is_project_client(p_project_id)
      )
  );
$$;
```

- [ ] **Step 3: Применить миграцию** (после Task 9)

Run: `Get-Content -Raw supabase/migrations/20260629_0008_marketplace_leak_fix.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: `DROP POLICY`, `CREATE POLICY`, `CREATE FUNCTION`.

- [ ] **Step 4: Тест — утечка закрыта; сотрудник и заказчик не сломаны**

```sql
-- чистый заказчик больше НЕ видит чужой marketplace прямым запросом
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select count(*) leak_after from public.projects where name='Чужой маркет-проект'; rollback;
-- сотрудник по-прежнему видит marketplace
begin; set local role authenticated; set local request.jwt.claims='{"sub":"6a16f4df-62df-4b9c-bf4f-2298bf61b5ba","role":"authenticated"}';
  select count(*) emp_sees from public.projects where name='Чужой маркет-проект'; rollback;
-- заказчик видит СВОЙ проект через RPC (Task 9 уже применён)
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select count(*) own_via_rpc from public.get_my_client_projects(); rollback;
```
Expected: `leak_after = 0`; `emp_sees = 1`; `own_via_rpc >= 1`.

- [ ] **Step 5: Commit**

```bash
git add supabase/migrations/20260629_0008_marketplace_leak_fix.sql
git commit -m "fix(db): закрыть marketplace-утечку — is_employee вместо is_approved (§7)"
```

---

## Task 9: Счётчик задач в `get_my_client_projects` (§6 индикатор)

**Files:**
- Create: `supabase/migrations/20260629_0009_client_projects_task_count.sql`

**Interfaces:**
- Modifies: `get_my_client_projects()` — добавить колонку `open_task_count int` (задачи в статусах кроме `Готово`/`Отменена`) и `visibility text` (чтобы фронт знал, выставлен ли проект на маркетплейс).

**Порядок:** применить ДО Task 8 (см. зависимость в Task 8).

- [ ] **Step 1: Написать миграцию**

`supabase/migrations/20260629_0009_client_projects_task_count.sql`:
```sql
-- 20260629_0009: индикатор задач + visibility в клиентской проекции (§6).
-- drop нужен: меняется тип возврата (добавлены visibility, open_task_count).
drop function if exists public.get_my_client_projects();
create or replace function public.get_my_client_projects()
returns table(id uuid, name text, stage text, start_date date, deadline date,
              contract_sum numeric, paid_amount numeric, executor text,
              visibility text, open_task_count int)
language sql stable security definer set search_path to 'public', 'pg_temp' as $$
  select p.id, p.name, p.stage, p.start_date, p.deadline,
         p.contract_sum, p.paid_amount, p.executor, p.visibility,
         (select count(*)::int from public.project_tasks t
          where t.project_id = p.id and t.status not in ('Готово','Отменена')) as open_task_count
  from public.projects p
  join public.clients c on c.id = p.client_id
  where c.user_id = auth.uid()
  order by p.created_at desc;
$$;
```

- [ ] **Step 2: Применить миграцию**

Run: `Get-Content -Raw supabase/migrations/20260629_0009_client_projects_task_count.sql | wsl docker exec -i supabase-db psql -U postgres -d postgres -v ON_ERROR_STOP=1`
Expected: `CREATE FUNCTION`.

- [ ] **Step 3: Тест — проекция содержит счётчик и visibility**

```sql
begin; set local role authenticated; set local request.jwt.claims='{"sub":"00000000-0000-4000-8000-00000000c111","role":"authenticated"}';
  select name, visibility, open_task_count from public.get_my_client_projects();
rollback;
```
Expected: строки тест-клиента, у «Тестовый заказ клиента» — `open_task_count` отражает незакрытые задачи, `visibility` присутствует.

- [ ] **Step 4: Commit**

```bash
git add supabase/migrations/20260629_0009_client_projects_task_count.sql
git commit -m "feat(db): счётчик задач и visibility в get_my_client_projects (§6)"
```

---

## Task 10: Фронт — обёртки DATA OPERATIONS

**Files:**
- Modify: `src/App.jsx` — блок DATA OPERATIONS (рядом с `fetchProjectComments` ~стр 433–471). **Править через PowerShell `[IO.File]::WriteAllText` (грабля F:).**

**Interfaces:**
- Consumes: RPC из Tasks 3,6,7,9.
- Produces (JS-функции, используют Tasks 11–12):
  - `fetchMyClientProjects(client) → [{id,name,stage,startDate,deadline,contractSum,paidAmount,executor,visibility,openTaskCount}]`
  - `createProjectRequest(client, {name,description,deadline,mode,assignmentMode,desiredExecutorId})  → requestId`
  - `acceptProjectRequest(client, requestId) → projectId`
  - `rejectProjectRequest(client, requestId, reason) → void`
  - `listAvailableExecutors(client) → [{id,name,position}]`
  - `fetchProjectRequests(client) → [{id,clientId,createdBy,name,description,desiredDeadline,mode,assignmentMode,desiredExecutorId,status,acceptedProjectId,createdAt}]`
  - `clientSetTaskStatus(client, taskId, status) → void`

- [ ] **Step 1: Прочитать актуальный блок DATA OPERATIONS, чтобы вставить рядом**

Run: прочитать `src/App.jsx` строки 610–630 (где `get_my_client_projects` уже вызывается) и 430–472. Убедиться, что `fetchMyClientProjects` ещё не существует под другим именем (строка 617 — внутри какой-то функции; переиспользовать/расширить её, а не плодить дубль).

- [ ] **Step 2: Добавить/расширить обёртки**

Вставить в блок DATA OPERATIONS (через `[IO.File]::WriteAllText` всего файла с этой вставкой):
```js
// ── Заказчик 2.0: проекты-заявки и приёмка ──────────────────────────────────
async function fetchMyClientProjects(client) {
  const { data, error } = await client.rpc("get_my_client_projects");
  if (error) throw error;
  return (data || []).map(r => ({
    id: r.id, name: r.name, stage: r.stage, startDate: r.start_date, deadline: r.deadline,
    contractSum: Number(r.contract_sum) || 0, paidAmount: Number(r.paid_amount) || 0,
    executor: r.executor || "", visibility: r.visibility, openTaskCount: Number(r.open_task_count) || 0,
  }));
}

async function listAvailableExecutors(client) {
  const { data, error } = await client.rpc("list_available_executors");
  if (error) throw error;
  return (data || []).map(r => ({ id: r.id, name: r.name || "", position: r.position || "" }));
}

async function createProjectRequest(client, { name, description = null, deadline = null,
    mode = "quick", assignmentMode = "marketplace", desiredExecutorId = null }) {
  const { data, error } = await client.rpc("create_project_request", {
    p_name: name, p_description: description, p_deadline: deadline, p_mode: mode,
    p_assignment_mode: assignmentMode, p_desired_executor_id: desiredExecutorId, p_client_id: null,
  });
  if (error) throw error;
  return data; // uuid заявки
}

async function acceptProjectRequest(client, requestId) {
  const { data, error } = await client.rpc("accept_project_request", { p_request_id: requestId });
  if (error) throw error;
  return data; // uuid проекта
}

async function rejectProjectRequest(client, requestId, reason = null) {
  const { error } = await client.rpc("reject_project_request", { p_request_id: requestId, p_reason: reason });
  if (error) throw error;
}

async function fetchProjectRequests(client) {
  const { data, error } = await client
    .from("project_requests").select("*").order("created_at", { ascending: false });
  if (error) throw error;
  return (data || []).map(r => ({
    id: r.id, clientId: r.client_id, createdBy: r.created_by, name: r.name,
    description: r.description, desiredDeadline: r.desired_deadline, mode: r.mode,
    assignmentMode: r.assignment_mode, desiredExecutorId: r.desired_executor_id,
    status: r.status, acceptedProjectId: r.accepted_project_id, createdAt: r.created_at,
  }));
}

async function clientSetTaskStatus(client, taskId, status) {
  const { error } = await client.rpc("client_set_task_status", { p_task_id: taskId, p_status: status });
  if (error) throw error;
}
```

- [ ] **Step 3: Сборка не падает**

Run: `npm run build`
Expected: build OK (нет синтаксических ошибок, обёртки не конфликтуют с существующими именами).

- [ ] **Step 4: Commit**

```bash
git add src/App.jsx
git commit -m "feat(web): DATA OPERATIONS обёртки для проектов-заявок и приёмки"
```

---

## Task 11: Фронт — портал заказчика (создание заявки, задачи, приёмка)

**Files:**
- Modify: `src/App.jsx` — клиентский портал (компонент, отвечающий за `isClientOnly`/`clientView`, ~стр 8778+). Править через `[IO.File]::WriteAllText`.

**Interfaces:**
- Consumes: `fetchMyClientProjects`, `createProjectRequest`, `listAvailableExecutors`, `clientSetTaskStatus`, `fetchTasks`, `fetchProjectComments`, `propose_tz_version`-обёртка (если есть; иначе через существующий механизм ТЗ).
- Produces: UI — кнопка «Создать проект» (модалка режим+путь подбора), индикатор `openTaskCount` на карточке, экран задач проекта с приёмкой.

**Right-sizing:** это самый крупный фронт-кусок. Разбит на под-шаги; каждый под-шаг проверяется визуально в `npm run dev`.

- [ ] **Step 1: Индикатор задач на карточке проекта заказчика**

Найти рендер карточек проектов в клиентском портале (использует результат `fetchMyClientProjects`). Добавить бейдж с `openTaskCount` (если > 0). Следовать стилю существующих карточек (`ReportCards`, ~стр 8246).
Verify: `npm run dev` → войти тест-клиентом → на карточке проекта виден счётчик задач.

- [ ] **Step 2: Модалка «Создать проект»**

Добавить кнопку «+ Создать проект» в портале и модалку с полями:
- Режим: радио `quick` / `detailed` (detailed показывает `description`, `desired_deadline`).
- Путь подбора: радио «🔍 Найти исполнителя (маркетплейс)» = `marketplace` / «👤 Назначить исполнителя» = `assignee`.
- При `assignee` — `<select>`, наполняемый `listAvailableExecutors` (label = `name` + (position?)).
- Кнопка «Отправить» → `createProjectRequest(...)` → тост «Заявка отправлена» → закрыть модалку.

Минимальный каркас модалки (встроить в стиль существующих модалок проекта):
```jsx
function CreateRequestModal({ supabase, onClose, onCreated }) {
  const [mode, setMode] = React.useState("quick");
  const [assignmentMode, setAssignmentMode] = React.useState("marketplace");
  const [name, setName] = React.useState("");
  const [description, setDescription] = React.useState("");
  const [deadline, setDeadline] = React.useState("");
  const [executors, setExecutors] = React.useState([]);
  const [desiredExecutorId, setDesiredExecutorId] = React.useState("");
  const [busy, setBusy] = React.useState(false);

  React.useEffect(() => {
    if (assignmentMode === "assignee" && executors.length === 0) {
      listAvailableExecutors(supabase).then(setExecutors).catch(() => setExecutors([]));
    }
  }, [assignmentMode]);

  async function submit() {
    if (!name.trim()) return;
    if (assignmentMode === "assignee" && !desiredExecutorId) return;
    setBusy(true);
    try {
      await createProjectRequest(supabase, {
        name: name.trim(),
        description: mode === "detailed" ? (description || null) : null,
        deadline: mode === "detailed" && deadline ? deadline : null,
        mode, assignmentMode,
        desiredExecutorId: assignmentMode === "assignee" ? desiredExecutorId : null,
      });
      onCreated?.();
      onClose();
    } finally { setBusy(false); }
  }
  // ... разметка модалки в стиле существующих (заголовок, поля по mode/assignmentMode, кнопки)
}
```
Verify: `npm run dev` → создать заявку обоими путями → проверить в БД, что `project_requests` пополнилась (`assignment_mode`, `desired_executor_id` корректны).

- [ ] **Step 3: Экран задач проекта заказчика + приёмка**

В клиентском портале при открытии проекта показать его задачи (`fetchTasks(client, { projectId })`). Для задач в статусе `На проверке` — кнопки «Принять» (`clientSetTaskStatus(client, taskId, "Готово")`) и «Вернуть» (`"В работе"`). Комментарии/ТЗ — переиспользовать существующие компоненты (`fetchProjectComments`, механизм ТЗ).
Verify: `npm run dev` → тест-клиент открывает «Тестовый заказ клиента» → видит задачу `На проверке` → «Принять» → статус становится `Готово` (проверить в БД).

- [ ] **Step 4: Сборка и коммит**

Run: `npm run build` → OK.
```bash
git add src/App.jsx
git commit -m "feat(web): портал заказчика — создание заявки, задачи, приёмка"
```

---

## Task 12: Фронт — интерфейс сотрудника (входящие заявки)

**Files:**
- Modify: `src/App.jsx` — раздел для сотрудника/админа. Править через `[IO.File]::WriteAllText`.

**Interfaces:**
- Consumes: `fetchProjectRequests`, `acceptProjectRequest`, `rejectProjectRequest`.
- Produces: список входящих заявок со статусом `Новая`, кнопки «Принять»/«Отклонить», бейдж пути подбора.

- [ ] **Step 1: Список входящих заявок**

Добавить вкладку/секцию «Заявки» (видна при `is_employee`/admin). Рендерить `fetchProjectRequests().filter(r => r.status === "Новая")`. Карточка заявки: имя, режим, бейдж «маркетплейс»/«исполнитель: <имя>», дата, кнопки.
- «Принять» → `acceptProjectRequest(client, r.id)` → тост «Проект создан» → обновить список.
- «Отклонить» → `rejectProjectRequest(client, r.id)` → обновить список.

Verify: `npm run dev` → войти сотрудником → видеть заявки тест-клиента → «Принять» marketplace-заявку → проект появляется в общем списке проектов (`fetchProjects`), `stage='Поиск исполнителя'`.

- [ ] **Step 2: Сборка и коммит**

Run: `npm run build` → OK.
```bash
git add src/App.jsx
git commit -m "feat(web): интерфейс сотрудника — входящие заявки (принять/отклонить)"
```

---

## Task 13: Верификация §7 на фронте (заказчик не зависит от прямого запроса projects)

**Files:**
- Inspect/Modify (если нужно): `src/App.jsx` — bootstrap-загрузка (`fetchProjects` ~стр 8578, 8648; `isClientOnly` ~стр 8778).

**Interfaces:** нет новых; цель — убедиться, что клиентский портал берёт проекты ТОЛЬКО из `fetchMyClientProjects`, а не из `fetchProjects` (после §7 заказчик не получит свои marketplace-проекты прямым запросом).

- [ ] **Step 1: Проверить, использует ли клиентский портал результат fetchProjects**

Прочитать bootstrap (`~стр 8560–8670`) и ветку рендера `clientView`. Установить, откуда `clientView` берёт список проектов.
- Если из `fetchMyClientProjects` — §7 безопасен, перейти к Step 3.
- Если из `fetchProjects` — баг, исправить (Step 2).

- [ ] **Step 2: (если нужно) Переключить клиентский портал на fetchMyClientProjects**

Для `isClientOnly` грузить проекты через `fetchMyClientProjects`, не `fetchProjects`. Через `[IO.File]::WriteAllText`.

- [ ] **Step 3: E2E-проверка изоляции в браузере**

`npm run dev`. Войти тест-клиентом, открыть DevTools → Network. Подтвердить: запросов вида `from('projects')` от клиента нет (только RPC `get_my_client_projects`). Заказчик видит свои проекты; чужие marketplace — не видит.

- [ ] **Step 4: Финальный регресс — сотруднические сценарии**

Войти сотрудником: список проектов, marketplace, «Взять в работу», задачи — работают как раньше (баг 6.2 не вернулся: создать задачу сотрудником, проверить видимость).

- [ ] **Step 5: Commit (если были правки)**

```bash
git add src/App.jsx
git commit -m "fix(web): клиентский портал берёт проекты через RPC, не прямой запрос (§7)"
```

---

## Cleanup (после приёмки фазы)

- [ ] Удалить тестовые данные Task 0: `delete from project_requests where name like '%тест%' or name like '%e2e%'; delete from projects where name in ('Тестовый заказ клиента','Чужой маркет-проект') or name like '%e2e%'; delete from project_tasks where title like '%Проверка ТЗ%' or title in ('x','y','z'); delete from user_roles where user_id='00000000-0000-4000-8000-00000000c111'; delete from clients where user_id='00000000-0000-4000-8000-00000000c111'; delete from profiles where id='00000000-0000-4000-8000-00000000c111'; delete from auth.users where id='00000000-0000-4000-8000-00000000c111';` — выполнять осознанно, проверив, что прод-данные не задеты.
- [ ] Удалить временные файлы `C:\Temp\test_*.sql`, `C:\Temp\seed_test_client.sql`.

---

## Self-Review (выполнено при написании плана)

**1. Spec coverage:**
- §2 ось 1 (видимость задач) → Task 2. ✓
- §2 ось 2 (комментарии/ТЗ) → Task 2 (B-1). ✓
- §2 ось 3 (назначение исполнителя/Взять в работу) → существующий `take_project`, регресс в Task 13. ✓
- §2 ось 4 (создание заявок, оба режима) → Tasks 5,7,11. ✓
- §2 ось 5 (приёмка) → Task 3,11. ✓
- §2 ось 6 (оба пути подбора) → Tasks 6,7,11. ✓
- §2 ось 7 (owner_id, 1 клик) → Task 7,12. ✓
- §4.0 is_employee → Task 1. ✓
- §4.1 доступ → Task 2; §4.2 приёмка → Task 3; §4.3 автоподтяг — UI-логика Task 11 (предзаполнение из executors). ✓
- §5.1/5.2 таблица+RLS → Task 5; §5.3 RPC → Task 7; §5.4 список → Task 6. ✓
- §6 UX (индикатор, модалка, задачи, сотрудник) → Tasks 9,11,12. ✓
- §7 утечка → Task 8 + верификация Task 13. ✓
- §8a И-1/И-2 → учтены в Task 1 (роль) и существующем `set_client_user`; верификация И-2 — cleanup-чек. ✓
- §10 критерии → распределены по тестам Tasks 1–9,13. ✓
- B-1 → Task 2; B-2 → Task 8/9/13; B-3 → Task 4. ✓
- M-1/M-3 → Task 7 (прямая вставка, формат executors, idempotency); M-2 → Task 1; M-4 → существующий код; m-2 → Task 5; m-3 → порядок Tasks 6→7. ✓

**2. Placeholder scan:** UI-разметка Task 11/12 описана каркасом + точными обёртками/сигнатурами (полная JSX-вёрстка следует существующим компонентам портала, на которые даны ссылки-строки) — это сознательное решение для монолита App.jsx, не плейсхолдер логики. Все SQL-шаги содержат полный код. `<CLIENT_PROJECT_UUID>`, `<CLIENT_TASK_UUID>` и пр. — это значения, фиксируемые исполнителем в Task 0/2 (помечено), не пропуски.

**3. Type consistency:** имена обёрток (`fetchMyClientProjects`, `createProjectRequest`, `acceptProjectRequest`, `rejectProjectRequest`, `listAvailableExecutors`, `fetchProjectRequests`, `clientSetTaskStatus`) и RPC-параметры (`p_name`, `p_assignment_mode`, `p_desired_executor_id`, `p_request_id`, `p_task_id`, `p_status`) согласованы между Tasks 3,6,7,10,11,12. Сигнатура `create_project_request(text,text,date,text,text,uuid,uuid)` совпадает в Task 7 (создание) и Task 10 (вызов).

**Все схемы сверены с живой БД** (projects, project_tasks, tasks/comments/tz RLS, clients, user_roles, notifications, ключевые функции). Плейсхолдеров-догадок в SQL нет.
