-- Должность пользователя (например «Главный инженер»). Показывается в карточке
-- профиля и видна участникам команды. Фронт пишет её прямым update profiles под RLS
-- (та же политика «обновление своей строки», что и для поля name).
alter table public.profiles
  add column if not exists position text;
